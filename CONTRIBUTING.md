# Contributing to Mizo Knowledge Vault

We welcome contributions! To add a new node:
1. Ensure the filename uses `Snake_Case_With_Spaces.md`.
2. Add a `[[Link]]` to at least 2 existing nodes to keep the graph connected.
3. Use tags like `#society`, `#science`, or `#history`.
4. verify linguistic accuracy in Mizo and English.
